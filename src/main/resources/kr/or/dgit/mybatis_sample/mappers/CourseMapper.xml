<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.dgit.mybatis_sample.dao.CourseDao">
	<resultMap type="Course" id="CourseResult">
		<id property="courseId" column= "course_id"/>
		<result property="name" column="name"/>
		<result property="description" column="description"/>
		<result property="startDate" column="start_date"/>
		<result property="endDate" column="end_date"/>
	</resultMap>
	
	<!-- if 조건 -->
	<sql id="courseSql">
		SELECT course_id, name, description, start_date, end_date, tutor_Id
		from courses
	</sql>
	<sql id="whereSql">
		<where>
			<if test="courseName != null">
				and name like #{courseName}
			</if>
			<if test="startDate != null">
				and start_date &gt;= #{startDate}
			</if>
			<if test="endDate != null">
				and end_date &lt;= #{endDate}
			</if>			
		</where>
	</sql>
	
	<select id="selectCoursesByConditionWithAPI" parameterType="map" resultMap="CourseResult">
		<include refid="courseSql" />
		<include refid="whereSql" />
	</select>
	
	<!-- choose 조건 -->
	<sql id="chooseSql">
		<choose>
			<when test="searchBy=='Tutor'">
				WHERE TUTOR_ID=#{tutorId}
			</when>
			<when test="searchBy=='CourseName'">
				WHERE name like #{courseName}
			</when>
			<otherwise>
				WHERE end_date &gt;=now()
			</otherwise>
		</choose>
	</sql>
	<select id="selectCaseCoursesWithAPI" parameterType="map" resultMap="CourseResult">
		<include refid="courseSql" />
		<include refid="chooseSql" />
	</select>
	
	
	<!-- where 조건 -->
	<sql id="whereSql2">
		<where>
			<if test="tutorId != null">
				TUTOR_ID = #{tutorId}
			</if>
			<if test="courseName != null">
				AND name like #{courseName}
			</if>
			<if test="startDate != null">
				AND start_date &gt;= ${startDate}
			</if>
			<if test="endDate != null">
				AND end_date &lt;=#{endDate}
			</if>
		</where>
	</sql>
	
	<select id="selectWhereCourses" parameterType="map" resultMap="CourseResult">
		<include refid="courseSql" />
		<include refid="whereSql2" />
	</select>
	
	
	<!-- trim 조건 -->
	<sql id="whereTrimSql">
		<trim prefix="WHERE" prefixOverrides="AND|OR">
			<!-- where 자동 추가 -->
			<if test="tutorId != null">
				TUTOR_ID=#{tutorId}
			</if>
			<!-- 만약 where tutor_id=1 이 추가 된 경우 where 삭제 -->
			<if test="courseName != null">
				AND name like #{courseName}
			</if>
		</trim>
	</sql>
	
	<select id="selectTrimCoursesWithAPI" parameterType="map" resultMap="CourseResult">
		<include refid="courseSql"/>
		<include refid="whereTrimSql"/>	
	</select>
	
	
	<!-- foreach 루프 -->
	
	<sql id="foreachSql">
		<if test="tutorIds != null">
			<where>
				tutor_id IN
				<foreach item="tutorId" collection="tutorIds" index="index" open="(" close=")" separator=",">
					#{tutorId}	
				</foreach>
			</where>
		</if>
	</sql>
	
	<select id="selectCoursesForeachByTutorsWithAPI" parameterType="map" resultMap="CourseResult">
		<include refid="courseSql"/>
		<include refid="foreachSql"/>	
	</select>
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
</mapper>